/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    domains: ['phyzioline.vercel.app', 'cdn.jsdelivr.net', 'cdnjs.cloudflare.com', 'code.jquery.com']
  }
}
